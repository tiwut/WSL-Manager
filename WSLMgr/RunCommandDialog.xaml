<Window x:Class="WSLMgr.RunCommandDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Run Command in WSL"
        Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource CustomWindowStyle}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="RUN COMMAND" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentLightBrush}"/>
            <TextBlock x:Name="TxtDistroName" Text="Inside Ubuntu" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,2,0,0"/>
        </StackPanel>


        <TextBlock Grid.Row="1" Text="Command to execute" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,5"/>
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtCommand" Grid.Column="0" FontSize="14" FontFamily="Consolas" VerticalAlignment="Center" KeyDown="TxtCommand_KeyDown" Tag="Enter command, e.g. uname -a"/>
            <Button x:Name="BtnRun" Grid.Column="1" Content="Execute" Style="{StaticResource PrimaryButtonStyle}" Margin="10,0,0,0" Padding="20,0" Height="40" Click="BtnRun_Click"/>
        </Grid>


        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Border Background="#070B14" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                <TextBox x:Name="TxtOutput"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="#10B981"
                         FontFamily="Consolas, Courier New"
                         FontSize="13"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Padding="10"/>
            </Border>
            <TextBlock x:Name="TxtPlaceholder" Text="Output will stream here..." Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas" FontSize="13" Margin="11,11,0,0" IsHitTestVisible="False"/>
        </Grid>


        <Grid Grid.Row="4">
            <Button Content="Close" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Right" Width="100" Click="BtnClose_Click"/>
        </Grid>
    </Grid>
</Window>
